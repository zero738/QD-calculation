# CP2K emits aligned lines with intentional trailing spaces. Preserve LF and
# do not treat those spaces as source-code whitespace errors.
interface_thickness_prototype/smoke_tests/**/output.out text eol=lf whitespace=-trailing-space
